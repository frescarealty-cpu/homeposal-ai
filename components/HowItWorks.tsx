/**
 * Re-export for imports that resolve to `/components/HowItWorks` at the repo root
 * (matches common tooling paths and avoids duplicate implementations).
 */
export { HowItWorks as default, HowItWorks } from "../src/components/HowItWorks";
